
from sikuli import *
import subprocess
subprocess.Popen(["notepad.exe"])
# wait# Open Google Chrome browser
# wait("address_bar.png")       # Wait for the address bar to become visible (take screenshot of your browser's address bar)
# click("address_bar.png")      # Click on the address bar to focus
# type("https://www.google.com" + Key.ENTER)  # Type Google URL and press Enter
# wait("google_home.png")